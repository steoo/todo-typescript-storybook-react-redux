import React from 'react';
import { TodosContainer as TodosComponent } from './todos';

export default {
  title: 'Todos',
  component: TodosComponent,
};

export const Todos = () => <TodosComponent></TodosComponent>;
